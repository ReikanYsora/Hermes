{
  "name": "hermes",
  "version": "0.2.0",
  "description": "HERMES - real-time entity state-change visualisation card for Home Assistant",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:watch": "node --test --watch \"tests/*.test.mjs\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "terser": "^5.47.1",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
